# What we'll normally do
# def double(x):
#     return x * 2

# Now the above's equivalent within a Lambda expression
double = lambda x: x * 2

double(2)
double(3)

# Other Lambda expressions
# double = lambda num: num * 2
# lambda x, y: x * y
